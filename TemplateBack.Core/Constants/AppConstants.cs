namespace TemplateBack.TemplateBack.Core.Constants;

/* Global constants — avoids magic strings across the codebase */
public static class AppConstants
{
    public const string DateFormat = "yyyy-MM-dd";
}